char foo() {
    return 'p';
}

int start() {
    []char str = ['h', 'e', 'l', 'l', 'o'];
    char fooi = str[1];
    str[3] = 'r';
    str[2] = 'r';
    str[1] = 'a';
    char a = (true) ? foo(): fooi;
    print(a);

    // struct boo hell = {2, 3, 4};

    print(str);
    return 0;
}
