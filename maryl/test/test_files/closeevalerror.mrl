char foo() {
    return 'p';
}

int start() {
    []char str = ['h', 'e', 'l', 'l', 'o'];
    char fooi = str[1];
    int one = 1;
    str[3] = 'r';
    str[2] = 'r';
    str[one] = 'a';
    char a = (true) ? foo(): fooi;
    print(a);

    // struct boo hell = {2, 3, 4};

    print(str);
    return 0;
}
