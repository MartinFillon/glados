name: "glados"

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
      - dev

env:
  MIRROR_URL: "git@github.com:EpitechPromo2027/B-FUN-500-PAR-5-2-glados-martin.fillon.git"

jobs:
  check_stack_build:
    runs-on: self-hosted
    name: Building stack
    container:
      image: epitechcontent/epitest-docker:latest

    steps:
      - name: Check out
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check stack install (LISP)
        run: |
          cd lisp
          sudo make
          sudo stack test --allow-different-user

      - name: Check stack install (Maryl)
        run: |
          cd maryl
          sudo make
          sudo stack test --allow-different-user

  push_to_mirror:
    needs: [check_stack_build]
    if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')}}
    name: "pushes all files to mirror specified"
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pixta-dev/repository-mirroring-action@v1
        with:
          target_repo_url: ${{ env.MIRROR_URL }}
          ssh_private_key: ${{ secrets.KEY }}
